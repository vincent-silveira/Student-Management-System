package application.model;

/**
 * Represents the type of operation performed on a form.
 *
 * <p>This enum is used to determine whether a form is used for
 * creating a new record or modifying an existing record.</p>
 *
 * @author Vincent
 * @version 1.0
 */
public enum FormType {
    ADD,
    UPDATE
}
